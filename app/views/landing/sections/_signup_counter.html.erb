<% count = local_assigns.fetch(:count) { Rails.cache.fetch("landing/signup_count", expires_in: 30.seconds) { User.count } } %>
<span id="rsvp_counter" class="rsvp-counter" data-count="<%= count %>"><%= number_with_delimiter(count) %></span>
